# 🚀 설정 가이드 - Gemini 2.5 Flash Image Preview

## ✅ Gemini Nano Banana 사용

**공식 모델명**: `gemini-2.5-flash-image-preview`

이 모델은 **텍스트로 이미지를 생성**할 수 있는 Google의 공식 이미지 생성 모델입니다.

---

## 📦 1단계: 패키지 설치

```bash
npm install
```

---

## 🔑 2단계: Gemini API 키 발급

### Google AI Studio에서 발급

1. https://aistudio.google.com/app/apikey 접속
2. "Get API Key" 클릭
3. 키 복사

### Billing 설정 (필수)

무료 한도를 초과했다면 Google Cloud에서 billing 설정:

1. https://console.cloud.google.com/billing 접속
2. "결제 계정 만들기" 클릭
3. 카드 정보 입력
4. 프로젝트에 결제 계정 연결

---

## ⚙️ 3단계: 환경 변수 설정

```bash
cp .env.example .env
```

`.env` 파일 편집:
```
GEMINI_API_KEY=여기에_Gemini_키_입력
PORT=3000
```

**단일 API 키로 대본 분석 + 이미지 생성 모두 가능!**

---

## 🚀 4단계: 실행

```bash
npm start
```

브라우저 접속:
```
http://localhost:3000
```

---

## 💰 비용 (10개 이미지 기준)

### Gemini 2.5 Flash Image Preview 가격:

```
입력 (프롬프트): $0.075 / 1M tokens
출력 (텍스트): $0.30 / 1M tokens  
출력 (이미지): $30.00 / 1M tokens
```

**10개 이미지 실제 비용:**
```
대본 분석 (입력): ~$0.001
대본 분석 (출력): ~$0.002
이미지 생성 (입력): ~$0.01
이미지 생성 (출력): ~$0.40 (10개 × 1,290 tokens)
----------------------------------------
총 비용: 약 $0.41 (약 550원)
```

**100개 이미지: 약 $4.10 (약 5,500원)**

---

## 📖 주요 특징

### ✅ 장점:
- 단일 API 키로 모든 작업
- 고품질 이미지 생성
- 텍스트 렌더링 우수
- 빠른 생성 속도
- Google 생태계 통합

### 📝 이미지 생성 방법:
```javascript
const model = genAI.getGenerativeModel({ 
  model: "gemini-2.5-flash-image-preview" 
});

const result = await model.generateContent("프롬프트");
const imageData = result.candidates[0].content.parts[0].inlineData.data;
```

---

## ⚠️ 중요 참고사항

1. **모델명 정확히 사용**: `gemini-2.5-flash-image-preview`
2. **별도 설정 불필요**: 프롬프트만 전달하면 이미지 생성
3. **Base64 출력**: `inlineData.data`에서 이미지 데이터 추출
4. **16:9 비율**: 프롬프트에 명시하면 자동 적용

---

## 🎯 공식 문서

더 자세한 정보:
```
https://ai.google.dev/gemini-api/docs/image-generation?hl=ko
```

---

## ✅ 완료!

설정이 완료되었습니다!

`npm start`로 서버를 실행하고 이미지를 생성하세요.
